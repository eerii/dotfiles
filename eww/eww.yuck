;; ·········
;; variables
;; ·········

(deflisten volume :initial "{}" "meh get --volume -m")
(deflisten battery :initial "{}" "meh get --battery -m")
(deflisten network :initial "{}" "meh get --network -m")
(deflisten workspaces :initial "[]" "~/.config/sway/bin/getwork.py")

(defvar topleft_vol_rev false)

;; ·······
;; modules
;; ·······

(include "./windows/bar.yuck")
(include "./windows/notifications.yuck")
