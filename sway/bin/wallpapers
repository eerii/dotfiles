#!/bin/sh

while true; do
    if ! pgrep "swww-daemon" >/dev/null; then
        swww-daemon &
    else
        DIR="${HOME}/Media/Pictures/Wallpapers/Scavengers"
        BG="$(find "$DIR" -name '*.jpg' -o -name '*.png' | shuf -n1)"
        swww img "$BG"
    fi
    sleep 1800
done
